//https://jestjs.io/docs/en/manual-mocks
const alertSum = require('./side-effect');

describe('alertSum', () => {

  alert = jest.fn();

  it('should print to a window alert with the sum of two numbers', () => {
    fail()
  });

});