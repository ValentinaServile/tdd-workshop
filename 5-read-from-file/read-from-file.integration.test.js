//https://nodejs.org/api/fs.html
//Write a file into the /tmp folder and assert on reading
//Hint: use -> fs.writeFileSync(file, data[, options])
const readFromFile = require('./read-from-file');
const fs = require('fs');

describe('readFromFile', () => {

  it('should read correct lines', () => {

  });

});